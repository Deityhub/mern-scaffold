{
  "name": "trial",
  "version": "1.0.0",
  "description": "A mern stack app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start-watch": "nodemon server.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build",
    "dev": "concurrently \"yarn start-watch\" \"cd client && yarn start\""
  },
  "author": "Ojini Jude",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^1.18.5"
  }
}
